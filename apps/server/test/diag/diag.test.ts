// Intentionally empty — a one-off diagnostic scaffold left over
// from the v0.4.0 coverage push. The diag ran fine and the
// results were applied to the real test files; this file must
// not contribute to the test run. Keeping it under version
// control (empty) is cheaper than carrying a `.gitignore`
// exception for the diag scratch directory.
